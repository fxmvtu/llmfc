<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 模型下载需要网络 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Vulkan / GPU 加速 — llama.cpp 可通过 GGML 使用 Vulkan 通用 GPU 计算 -->
    <uses-feature android:glEsVersion="0x00030000" android:required="false" />

    <application>
        <!--
          AIComposePlugin is a FcitxPluginService (extends Service), registered here
          so the fcitx5 IME core can discover and bind to it at runtime.
        -->
        <service
            android:name=".AIComposePlugin"
            android:exported="true" />

        <!-- 设置页 Activity（后续实现） -->
        <activity
            android:name=".ui.AIComposeSettingsActivity"
            android:exported="false"
            android:label="@string/settings_title" />
    </application>
</manifest>
